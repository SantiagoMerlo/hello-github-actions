name: "Hello Actions"
description: "Greet someone"
author: "santiagomerlo@github.com"

inputs:
  santiago:
    description: "Who to greet"
    required: true
    default: "World"

runs:
  using: "docker"
  image: "Dockerfile"

branding:
  icon: "mic"
  color: "purple"
